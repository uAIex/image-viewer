From this folder, run:

```sh
swift run OCRImageViewer
```

The app starts a local server and opens:

```text
http://127.0.0.1:3000/
```

Click `Open Folder`, choose a folder of images, then browse them.
